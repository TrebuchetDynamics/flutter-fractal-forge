# Rendering engineering docs

Owns renderer architecture, backend capability policy, CPU/GPU tradeoffs, formula coverage, and AR rendering research.

Exposes decisions and playbooks for rendering behavior. It must not own local setup, benchmark results, or emulator validation logs.
