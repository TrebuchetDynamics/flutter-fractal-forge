# Research not needed

Local initial-view issue for `multibrot10`.

Source/test evidence:
- At center `(0, 0)` and zoom `1.0`, the degree-10 Multibrot viewport sits inside the compressed central body and renders all interior/black.
- Added a curated default preset at center `(0, 0)` with zoom `0.5` so the decagonal boundary is visible immediately.
- Added CPU recurrence coverage for `multibrot10` and a focused default-view render test.
