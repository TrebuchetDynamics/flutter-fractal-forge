// GENERATED — DO NOT EDIT BY HAND.
import 'package:meta/meta.dart';

@immutable
class {{ class_name }}Preset {
  final String id;
  final String name;
  final Map<String, double> params;
  const {{ class_name }}Preset({required this.id, required this.name, required this.params});
}

class {{ class_name }}Presets {
{% for p in presets %}
  static const {{ class_name }}Preset {{ p.id | lower_camel }} = {{ class_name }}Preset(
    id: '{{ p.id | dart_str }}',
    name: '{{ p.name | dart_str }}',
    params: {
{% for k, v in p.params.items() %}
      '{{ k }}': {{ v }},
{% endfor %}
    },
  );
{% endfor %}

  static const List<{{ class_name }}Preset> all = [
{% for p in presets %}
    {{ p.id | lower_camel }},
{% endfor %}
  ];
}
